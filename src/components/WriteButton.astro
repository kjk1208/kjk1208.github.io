---
const GITHUB_OWNER = 'kjk1208';           // 깃허브 사용자/조직명
const REPO = 'kjk1208.github.io';         // 레포 이름
const BRANCH = 'main';                    // 기본 브랜치
// GitHub "새 파일 만들기" URL (posts 폴더 대상)
const NEW_POST_URL = `https://github.com/${GITHUB_OWNER}/${REPO}/new/${BRANCH}/src/content/posts`;
---

<a
  href={NEW_POST_URL}
  class="inline-flex items-center gap-2 rounded-lg border px-3 py-2 text-sm hover:bg-gray-50"
  rel="noopener noreferrer"
  target="_blank"
>
  ✍️ 글쓰기(GitHub)
</a>